{
  "name": "coreai-utilities",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:tools": "pnpm --filter ./apps/tools dev",
    "dev:views": "pnpm --filter ./apps/views dev",
    "build": "pnpm -r run build",
    "build:ui": "pnpm --filter @myorg/ui run build",
    "build:tools": "pnpm --filter ./apps/tools run build",
    "build:views": "pnpm --filter ./apps/views run build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.5.1",
    "path": "^0.12.7",
    "vite": "^6.3.5"
  }
}
